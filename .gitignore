# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS junk
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# Plugin per-vault data (do not commit secrets or per-device state)
data.json

# Settings export file — contains PAT + S3 keys in plaintext.
forge-config.json

# NOTE: main.js / styles.css / manifest.json ARE intentionally committed at
# the repo root — BRAT and Obsidian both fetch them from the default branch
# at runtime.
