name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: lint + test + build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        # --omit=optional skips the @esbuild/* platform-specific binaries
        # that don't match the current OS/CPU — npm ci is strict about
        # them otherwise, and the host (Linux x64) doesn't need the
        # netbsd / windows / etc. variants to run lint + test + build.
        run: npm ci --omit=optional

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build (verify bundle compiles)
        run: npm run build

      - name: Check bundle size
        run: |
          size=$(stat -c%s main.js)
          echo "main.js: ${size} bytes"
          # Keep bundle under 80 KB; v0.1 baseline was ~48 KB.
          if [ "$size" -gt 81920 ]; then
            echo "::error::Bundle exceeds 80 KB ($size bytes) — investigate"
            exit 1
          fi
